import { Postion } from './type';

type PositionParams = {
  top: number;
  left: number;
  width: number;
  height: number;
  positon: Postion;
};
const calcPosition = (params: PositionParams) => {
  const calFn = {
    top: (params: PositionParams) => {},
  };
  return {};
};
