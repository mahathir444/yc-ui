import { getComponentSizeMap, getComponentShapeMap } from '@shared/utils/compt';

export const SIZE_CLASS = getComponentSizeMap('yc-switch', ['medium', 'small']);

export const SHAPE_CLASS = getComponentShapeMap('yc-switch', [
  'circle',
  'round',
  'line',
]);
