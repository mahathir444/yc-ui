import { getComponentShapeMap, getComponentSizeMap } from '@shared/utils/compt';
// button
export const SIZE_CLASS = getComponentSizeMap('yc-skeleton-shape', [
  'small',
  'medium',
  'large',
]);

export const SHAPE_CLASS = getComponentShapeMap('yc-skeleton-shape');
